# Root-level examples
# Copy values into backend/.env and frontend/.env.local before running the app.

# backend/.env
PORT=4000
APP_ENV=development
CORS_ORIGIN=http://localhost:3000

# frontend/.env.local
PORT=3000
API_PROXY_TARGET=http://127.0.0.1:4000
NEXT_PUBLIC_APP_NAME=Live CRUD Workspace
